<view class="panel {{isLastChild ? 'panel--is-last' : '' }}">
  <navigator url="{{url}}" hover-class="weui-cell_active">
    <view class="panel-hd">
      <view>{{title}}</view>
      <view class="arrow-right"></view>
    </view>
  </navigator>
  <slot></slot>
</view>
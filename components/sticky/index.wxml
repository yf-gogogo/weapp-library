<view class="sticky--selected sticky__wrapper" style="{{isSticky ? 'height: ' + wrapperHeight + 'px;' : ''}}">
  <view class="sticky--selected {{isSticky ? 'is-sticky' : ''}}" style="z-index: {{zIndex}};">
    <slot></slot>
  </view>
</view>